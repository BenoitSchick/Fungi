data_plot = list()
data_plot = [[0],[0],[0],[0],[0],[0],[0],[0]]
first = 1
while 1:
    data = conn.recv(BUFFER_SIZE)
    if not data: break
    print ("received data:", data)
    last_i = 0
    ch = 0
    for i in range(0,len(data)):
        if(data[i] == 0):
            data_plot[ch].append(int(data[last_i:i]))
            print (data_plot)
            last_i = i + 1
        if(data[i] == 32):
            last_i = i + 1
            if(ch < 7): ch += 1
            else: ch = 0
            if(first == 1):
                for k in range(0, 8):
                    del data_plot[k][0]
                first = 0


temp = list()
data_plot = list()
while 1:
    data = conn.recv(BUFFER_SIZE)
    if not data: break
    print ("received data:", data)
    last_i = 0
    ch = 0
    for i in range(0,len(data)):
        if(data[i] == 0 or data[i] == 32):
            temp.append(int(data[last_i:i]))
            print(temp)
            last_i = i + 1