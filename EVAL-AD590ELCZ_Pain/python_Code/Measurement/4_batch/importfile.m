function COM4Measurements = importfile(filename, dataLines)
%IMPORTFILE Import data from a text file
%  COM4MEASUREMENTS = IMPORTFILE(FILENAME) reads data from text file
%  FILENAME for the default selection.  Returns the numeric data.
%
%  COM4MEASUREMENTS = IMPORTFILE(FILE, DATALINES) reads data for the
%  specified row interval(s) of text file FILENAME. Specify DATALINES as
%  a positive scalar integer or a N-by-2 array of positive scalar
%  integers for dis-contiguous row intervals.
%
%  Example:
%  COM4Measurements = importfile("D:\SWITCHdrive\Work_Projects\EVAL-AD590ELCZ_Test\python_Code\Measurement\1_batch\COM4_Measurements.csv", [1, Inf]);
%
%  See also READTABLE.
%
% Auto-generated by MATLAB on 23-Nov-2021 10:15:15

%% Input handling

% If dataLines is not specified, define defaults
if nargin < 2
    dataLines = [1, Inf];
end

%% Set up the Import Options and import the data
opts = delimitedTextImportOptions("NumVariables", 4);

% Specify range and delimiter
opts.DataLines = dataLines;
opts.Delimiter = ";";

% Specify column names and types
opts.VariableNames = ["VarName1", "VarName2", "VarName3", "MonNov221228462021"];
opts.VariableTypes = ["double", "double", "double", "double"];

% Specify file level properties
opts.ImportErrorRule = "omitvar";
opts.MissingRule = "omitvar";
opts.ExtraColumnsRule = "ignore";
opts.EmptyLineRule = "read";
opts.ConsecutiveDelimitersRule = "join";

% Specify variable properties
opts = setvaropts(opts, "MonNov221228462021", "TrimNonNumeric", true);
opts = setvaropts(opts, "MonNov221228462021", "ThousandsSeparator", ",");

% Import the data
COM4Measurements = readtable(filename, opts);

%% Convert to output type
COM4Measurements = table2array(COM4Measurements);
end